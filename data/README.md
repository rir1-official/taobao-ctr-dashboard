# Data Notes

`data/raw/user_ad_data.csv` is a small sample kept only so the Streamlit pages
can run without the full original dataset.

`data/processed/` contains aggregated analysis outputs generated during the
course project. These files are small enough for demonstration and do not include
the complete raw dataset.
